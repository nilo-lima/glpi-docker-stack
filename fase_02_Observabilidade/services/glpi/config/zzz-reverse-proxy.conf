# Informa ao PHP que a requisição original era HTTPS (terminação TLS feita pelo Caddy).
# SetEnvIf é seguro aqui porque glpi-app só recebe tráfego de Caddy via rede interna Docker.
SetEnvIf X-Forwarded-Proto "^https$" HTTPS=on
